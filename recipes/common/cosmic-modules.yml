modules:
    - type: rpm-ostree
      install:
        - NetworkManager-tui
        - NetworkManager-openvpn
        - cosmic-store
        - cosmic-player
    - type: systemd
      system:
        enabled:
          - cosmic-greeter