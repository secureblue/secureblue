type: dnf
repos: 
  copr:
  - secureblue/hardened_malloc
install:
  install-weak-deps: false
  packages:
    - hardened_malloc

    # missing upstream
    - openssl
    - just
    - patch
    - p7zip
    - distrobox
    - toolbox
    - podman
    - nvme-cli
    - hdparm
    
    # signing deps 
    - sbsigntools